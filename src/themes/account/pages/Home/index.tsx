import { observer } from 'mobx-react';
import { Row } from 'react-bootstrap';

const HomePage = () => {
  return (
    <>
      <Row>Home Page</Row>
    </>
  );
};

export default observer(HomePage);
